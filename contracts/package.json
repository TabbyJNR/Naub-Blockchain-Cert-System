{
  "name": "naub-certificate-registry-contracts",
  "version": "1.0.0",
  "description": "CertificateRegistry smart contract for the NAUB Blockchain Certificate System (Chapter 3)",
  "license": "MIT",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "coverage": "hardhat coverage",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^17.4.2",
    "solc": "^0.8.24"
  }
}
